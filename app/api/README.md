# API routes

This folder is reserved for future Next.js server routes.

Planned routes will retrieve current platform analytics without exposing API
credentials to the browser. Phase 1 intentionally contains no active API route.
